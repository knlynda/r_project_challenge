<table>
  <% @packages.each do |package| %>
    <tr>
      <td><%= link_to(package.name, package_path(package)) %></td>
      <td><%= package.latest_version_number %></td>
    </tr>
  <% end %>
</table>
